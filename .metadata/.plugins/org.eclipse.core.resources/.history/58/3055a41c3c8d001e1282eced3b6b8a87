import java.util.Scanner;

public class SantiagoExamen {

	public static void main(String[] args) {
		Scanner sc = new Scanner(System.in);

		// declaro las variables de entrada a la matriz
		System.out.println("Declaramos el tamaño de la matriz");
		int m = 4;
		int n = 4;
		
		// creamos la matriz
		int[][] matriz = new int [m][n];
		
		// Obtener el número de filas y columnas que hay
		int nFilas = matriz.length;
		int nColumnas = matriz[0].length;
		System.out.println(nColumnas);		
		
		for(int i = 0; i < matriz.length; i++) {
			
		}
		
	}

}
